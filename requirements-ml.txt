# Parent cloudforge venv (/data/home/.venv) — ML, geo, and CV runtime.
#
# Web stack (FastAPI, uvicorn, SQLAlchemy, etc.) belongs in project-specific
# venvs (e.g. 00_GUI/.venv). Link the parent ML packages with:
#   bash scripts/link-shared-ml-venv.sh /data/projects/<repo>/00_GUI/.venv
#
# torch, torchvision, and detectron2 are installed by init-venv.sh (pinned versions).

# Headless build — no libGL.so.1 required in Docker/server environments
opencv-python-headless>=4.13
rasterio>=1.3
Pillow>=10.0
shapely>=2.0

numpy
pandas
matplotlib
ipython
jupyter
